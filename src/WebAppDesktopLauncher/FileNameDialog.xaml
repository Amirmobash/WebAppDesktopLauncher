<Window x:Class="WebAppDesktopLauncher.FileNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save file"
        Width="520"
        Height="190"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#F3F3F3">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Please enter file name:"
                   FontFamily="Segoe UI"
                   FontSize="12"
                   Foreground="#333" />

        <TextBox x:Name="FileNameBox"
                 Grid.Row="1"
                 Margin="0,8,0,0"
                 Height="30"
                 FontFamily="Segoe UI"
                 FontSize="12"
                 VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">

            <Button Width="90" Height="30" Margin="0,0,8,0"
                    Content="Cancel" Click="Cancel_Click"/>

            <Button Width="90" Height="30"
                    Content="Save" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
